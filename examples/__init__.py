"""Agents as their own platforms leave them on disk, for agent-red to be pointed at.

Nothing here is part of the product. Each directory holds what an install wizard writes when
somebody installs a skill, which is what the reader in `agentred.ingest` takes as its input.
"""
